# Personalized aliases file
#
# Shortcuts and functions that take arguments

# some more ls aliases
alias ll='ls -AlFX'
alias la='ls -A'
alias l='ls -CF'

# Add some helpful aliases
alias l.='ls -d .* --color=auto'
